# Completion Reports

Each completed queue item receives `DWI-*.md` containing starting and ending commits, files changed, acceptance results, evidence IDs/paths, tests, limitations, release impact, migrations/configuration changes, remaining severity inventory, screenshots where UI changed, and the exact next eligible item.

A completion report cannot substitute for missing required evidence.
