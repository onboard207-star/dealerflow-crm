import{describe,expect,it,vi}from"vitest";import{GlobalSearchQueryError,GlobalSearchReader}from"./global-search";
const source=()=>({customers:vi.fn().mockResolvedValue([{id:"cus_jordan1",displayName:"Jordan Lee",status:"active",email:"jordan@example.com"}]),leads:vi.fn().mockResolvedValue([{id:"lea_jordan1",customerId:"cus_jordan1",customerName:"Jordan Lee",stage:"contacted",status:"working",source:"website"}]),inventory:vi.fn().mockResolvedValue([{inventoryId:"inv_crv123",stockNumber:"H26418",vin:"1HGBH41JXMN109186",year:2026,make:"Honda",model:"CR-V",status:"available"}]),deals:vi.fn().mockResolvedValue([])});
const scope={userId:"usr_salesperson",organizationId:"org_dealerflow",locationIds:["loc_rooftop1"],capabilities:["customer.read","lead.read"]as const};
describe("GlobalSearchReader",()=>{it("searches only capability-authorized domains",async()=>{const sources=source();const result=await new GlobalSearchReader(sources).search(scope,"Jordan");expect(result.map(item=>item.kind)).toEqual(["customer","lead"]);expect(sources.inventory).not.toHaveBeenCalled();expect(result[0]?.href).toBe("/organizations/org_dealerflow/customers/cus_jordan1");});it("rejects unbounded or non-meaningful queries",async()=>{await expect(new GlobalSearchReader(source()).search(scope,"x")).rejects.toBeInstanceOf(GlobalSearchQueryError);await expect(new GlobalSearchReader(source()).search(scope,"valid",11)).rejects.toThrow("between 1 and 10");});});
