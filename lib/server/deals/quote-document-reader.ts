import type{DatabasePool}from"@/lib/server/database";import{withTenantDatabaseContext}from"@/lib/server/database";
export interface QuoteDocument{quote:{id:string;version:number;status:string;purchaseType:string;currency:string;subtotalCents:number;feeCents:number;taxCents:number;discountCents:number;totalCents:number;createdAt:string;expiresAt?:string;presentedAt?:string;acceptedAt?:string};deal:{id:string;dealNumber:string};customer:{id:string;displayName:string};vehicle:{label:string;vin:string;stockNumber?:string};location:{id:string;name:string};lines:readonly{id:string;position:number;category:string;description:string;quantity:number;unitAmountCents:number;totalCents:number}[]}
export class QuoteDocumentReader{constructor(private readonly pool:DatabasePool){}read(scope:{userId:string;organizationId:string;locationIds:readonly string[]|"all"},quoteId:string):Promise<QuoteDocument|null>{return withTenantDatabaseContext(this.pool,{userId:scope.userId,organizationId:scope.organizationId},async client=>{const all=scope.locationIds==="all";const locations=all?[]:scope.locationIds;const result=await client.query(`SELECT q.id,q.version,q.status::text,q.purchase_type::text,q.currency,q.subtotal_cents,q.fee_cents,q.tax_cents,q.discount_cents,q.total_cents,q.created_at,q.expires_at,q.presented_at,q.accepted_at,d.id deal_id,d.deal_number,d.customer_id,c.display_name,v.vin,v.year,v.make,v.model,v.trim,i.stock_number,d.location_id,location.name location_name FROM deal_quotes q JOIN deals d ON d.organization_id=q.organization_id AND d.id=q.deal_id JOIN customers c ON c.organization_id=d.organization_id AND c.id=d.customer_id JOIN vehicles v ON v.organization_id=d.organization_id AND v.id=d.primary_vehicle_id JOIN locations location ON location.organization_id=d.organization_id AND location.id=d.location_id LEFT JOIN inventory_units i ON i.organization_id=d.organization_id AND i.id=d.inventory_unit_id WHERE q.organization_id=$1 AND q.id=$2 AND ($3 OR d.location_id=ANY($4::text[])) LIMIT 1`,[scope.organizationId,quoteId,all,locations])as{rows:Array<Row>};const row=result.rows[0];if(!row)return null;const lines=await client.query("SELECT id,position,category::text,description,quantity,unit_amount_cents,total_cents FROM deal_quote_lines WHERE organization_id=$1 AND quote_id=$2 ORDER BY position",[scope.organizationId,quoteId])as{rows:Array<Line>};return{quote:{id:row.id,version:row.version,status:row.status,purchaseType:row.purchase_type,currency:row.currency,subtotalCents:row.subtotal_cents,feeCents:row.fee_cents,taxCents:row.tax_cents,discountCents:row.discount_cents,totalCents:row.total_cents,createdAt:row.created_at.toISOString(),...(row.expires_at?{expiresAt:row.expires_at.toISOString()}:{}),...(row.presented_at?{presentedAt:row.presented_at.toISOString()}:{}),...(row.accepted_at?{acceptedAt:row.accepted_at.toISOString()}:{})},deal:{id:row.deal_id,dealNumber:row.deal_number},customer:{id:row.customer_id,displayName:row.display_name},vehicle:{label:`${row.year} ${row.make} ${row.model}${row.trim?` ${row.trim}`:""}`,vin:row.vin,...(row.stock_number?{stockNumber:row.stock_number}:{})},location:{id:row.location_id,name:row.location_name},lines:lines.rows.map(item=>({id:item.id,position:item.position,category:item.category,description:item.description,quantity:item.quantity,unitAmountCents:item.unit_amount_cents,totalCents:item.total_cents}))};});}}
type Row={id:string;version:number;status:string;purchase_type:string;currency:string;subtotal_cents:number;fee_cents:number;tax_cents:number;discount_cents:number;total_cents:number;created_at:Date;expires_at:Date|null;presented_at:Date|null;accepted_at:Date|null;deal_id:string;deal_number:string;customer_id:string;display_name:string;vin:string;year:number;make:string;model:string;trim:string|null;stock_number:string|null;location_id:string;location_name:string};type Line={id:string;position:number;category:string;description:string;quantity:number;unit_amount_cents:number;total_cents:number};
