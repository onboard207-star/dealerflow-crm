import { describe, expect, it, vi } from "vitest";
import type { DatabaseClient, DatabasePool } from "@/lib/server/database";
import { DealDirectoryError, DealDirectoryReader } from "./deal-directory";
describe("DealDirectoryReader", () => { it("applies membership locations inside the query", async () => { const query = vi.fn<DatabaseClient["query"]>().mockResolvedValueOnce({}).mockResolvedValueOnce({}).mockResolvedValueOnce({ rows: [] }).mockResolvedValueOnce({}); const client: DatabaseClient = { query, release: vi.fn() }; const pool: DatabasePool = { connect: vi.fn().mockResolvedValue(client) }; await new DealDirectoryReader(pool).list({ userId: "usr_salesperson", organizationId: "org_dealerflow", locationIds: ["loc_main"] }, { status: "working" }); expect(query.mock.calls[2]?.[0]).toContain("d.location_id = ANY($3::text[])"); expect(query.mock.calls[2]?.[1]).toEqual(["org_dealerflow", false, ["loc_main"], "working", "", 50]); }); it("rejects invalid filters before database access", () => { const pool = { connect: vi.fn() } as unknown as DatabasePool; expect(() => new DealDirectoryReader(pool).list({ userId: "usr_salesperson", organizationId: "org_dealerflow", locationIds: "all" }, { status: "unknown" })).toThrow(DealDirectoryError); expect(pool.connect).not.toHaveBeenCalled(); }); });
