"use client";
import{useState,type FormEvent}from"react";import{ImagePlus}from"lucide-react";import{useRouter}from"next/navigation";import{Button}from"@/components/ui/button";
export function InventoryMediaUpload({organizationId,inventoryUnitId}:{organizationId:string;inventoryUnitId:string}){const router=useRouter();const[pending,setPending]=useState(false);const[message,setMessage]=useState<string>();async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=event.currentTarget,file=new FormData(form).get("photo"),altText=value(new FormData(form).get("altText"));if(!(file instanceof File)||!file.size){setMessage("Choose a photo to upload.");return;}setPending(true);setMessage(undefined);try{const base=`/api/organizations/${organizationId}/inventory/${inventoryUnitId}/media/uploads`;const initiation=await fetch(base,{method:"POST",headers:{"content-type":"application/json","idempotency-key":`media:${crypto.randomUUID()}`},body:JSON.stringify({filename:file.name,contentType:file.type,byteSize:file.size,altText})});if(!initiation.ok)throw new Error(await problem(initiation));const upload=await initiation.json()as{uploadId:string;url:string;headers:Record<string,string>};const stored=await fetch(upload.url,{method:"PUT",headers:upload.headers,body:file});if(!stored.ok)throw new Error("The storage provider rejected the upload. Check the R2 CORS policy and try again.");const completion=await fetch(`${base}/${upload.uploadId}`,{method:"POST"});if(!completion.ok)throw new Error(await problem(completion));setMessage("Photo verified and published.");form.reset();router.refresh();}catch(error){setMessage(error instanceof Error?error.message:"The photo could not be uploaded.");}finally{setPending(false);}}return <form className="mt-4 rounded-xl border bg-card p-4 shadow-soft" onSubmit={submit}><div className="flex items-center gap-2"><ImagePlus aria-hidden="true" className="size-5 text-muted-foreground"/><h2 className="font-medium">Add verified unit photo</h2></div><div className="mt-3 grid gap-3 sm:grid-cols-[minmax(0,1fr)_minmax(0,1fr)_auto] sm:items-end"><label className="text-xs font-medium text-muted-foreground">Photo<input accept="image/jpeg,image/png,image/webp" className="focus-ring mt-1 block h-11 w-full rounded-lg border bg-background px-3 py-2 text-sm text-foreground file:mr-3 file:border-0 file:bg-transparent file:text-sm file:font-medium" name="photo" required type="file"/></label><label className="text-xs font-medium text-muted-foreground">Accessible description<input className="focus-ring mt-1 h-11 w-full rounded-lg border bg-background px-3 text-sm text-foreground" maxLength={300} name="altText" placeholder="Front three-quarter view" required/></label><Button disabled={pending} type="submit">{pending?"Verifying…":"Upload photo"}</Button></div><p className="mt-2 text-xs text-muted-foreground">JPEG, PNG, or WebP. Maximum 20 MB. The photo is published only after server verification.</p><p aria-live="polite" className="mt-2 text-sm text-muted-foreground" role="status">{message}</p></form>;}function value(input:FormDataEntryValue|null){return typeof input==="string"?input.trim():"";}async function problem(response:Response){const payload=await response.json().catch(()=>undefined)as{message?:unknown;issues?:unknown}|undefined;if(Array.isArray(payload?.issues)&&payload.issues.every(item=>typeof item==="string"))return payload.issues.join(" ");return typeof payload?.message==="string"?payload.message:"The request could not be completed.";}
