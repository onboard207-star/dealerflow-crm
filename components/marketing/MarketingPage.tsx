import type { LucideIcon } from "lucide-react";
import { MarketingShell, FeatureCard, PageHero } from "./MarketingShell";

export interface MarketingFeature {readonly icon:LucideIcon;readonly title:string;readonly description:string}
export function MarketingPage({eyebrow,title,description,features,note}:{eyebrow:string;title:string;description:string;features:readonly MarketingFeature[];note?:string}){return <MarketingShell><PageHero eyebrow={eyebrow} title={title} description={description} primary={{href:"/book-demo",label:"Book a demo"}} secondary={{href:"/product",label:"Explore the product"}}><div className="rounded-2xl border bg-card p-6 shadow-soft"><p className="text-sm font-medium">What to expect</p><ul className="mt-4 space-y-3 text-sm text-muted-foreground">{features.slice(0,4).map(item=><li className="flex gap-3" key={item.title}><span className="mt-2 size-1.5 shrink-0 rounded-full bg-primary"/>{item.title}</li>)}</ul></div></PageHero><section className="mx-auto max-w-6xl px-4 pb-20 sm:px-6"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{features.map(item=><FeatureCard icon={item.icon} title={item.title} key={item.title}>{item.description}</FeatureCard>)}</div>{note?<p className="mt-8 rounded-xl border bg-muted p-4 text-sm text-muted-foreground">{note}</p>:null}</section></MarketingShell>}
