import{NextResponse}from"next/server";import{AuthenticationError,MembershipError,PostgresMembershipReader,authenticateOrganizationRequest}from"@/lib/server/auth";import{getDatabasePool}from"@/lib/server/database";import{GlobalSearchQueryError,GlobalSearchReader}from"@/lib/server/search";
export const dynamic="force-dynamic";interface Context{params:Promise<{organizationId:string}>}
export async function GET(request:Request,context:Context){try{const{organizationId}=await context.params;const pool=getDatabasePool();const actor=await authenticateOrganizationRequest(request,organizationId,new PostgresMembershipReader(pool));const membership=actor.memberships[0]!;const url=new URL(request.url);const results=await GlobalSearchReader.postgres(pool).search({userId:actor.userId,organizationId,locationIds:membership.locationIds,capabilities:membership.capabilities},url.searchParams.get("q")??"",number(url.searchParams.get("limit"))??5);return NextResponse.json({results},{headers:{"cache-control":"private, no-store"}});}catch(error){if(error instanceof AuthenticationError)return problem(401,"unauthorized",error.message);if(error instanceof MembershipError)return problem(403,"forbidden","Organization search is not permitted.");if(error instanceof GlobalSearchQueryError)return problem(400,"invalid_request",error.message);return problem(500,"internal_error","Search could not be completed.");}}
function number(value:string|null){if(value===null)return undefined;const parsed=Number(value);return Number.isInteger(parsed)?parsed:NaN;}function problem(status:number,error:string,message:string){return NextResponse.json({error,message},{status,headers:{"cache-control":"no-store"}});}
