import{randomUUID}from"node:crypto";import{NextResponse}from"next/server";import{InventoryLifecycleConflictError,InventoryLifecycleService,InventoryLifecycleValidationError}from"@/lib/application/vehicles";import{AuthorizationError}from"@/lib/platform/auth";import{AuthenticationError,MembershipError,PostgresMembershipReader,authenticateOrganizationRequest}from"@/lib/server/auth";import{getDatabasePool}from"@/lib/server/database";import{PostgresInventoryLifecycleProvider}from"@/lib/server/vehicles";export const runtime="nodejs";export const dynamic="force-dynamic";interface Context{params:Promise<{organizationId:string;inventoryUnitId:string}>}export async function PATCH(request:Request,context:Context){const correlationId=request.headers.get("x-correlation-id")?.trim()||`req_${randomUUID()}`;try{const{organizationId,inventoryUnitId}=await context.params;const body=object(await request.json());const idempotencyKey=request.headers.get("idempotency-key")?.trim();if(!idempotencyKey)return problem(400,"invalid_request","Idempotency-Key is required.",correlationId);const status=optionalStatus(body.status);const pool=getDatabasePool();const actor=await authenticateOrganizationRequest(request,organizationId,new PostgresMembershipReader(pool));const result=await new InventoryLifecycleService(new PostgresInventoryLifecycleProvider(pool,{userId:actor.userId,organizationId})).update({actor,organizationId,correlationId,idempotencyKey,inventoryUnitId,expectedUpdatedAt:string(body.expectedUpdatedAt),listPriceCents:number(body.listPriceCents),...(status?{status}:{}),...(optional(body.reason)?{reason:optional(body.reason)}:{})});return NextResponse.json(result,{status:200,headers:{"cache-control":"private, no-store","x-correlation-id":correlationId}});}catch(error){if(error instanceof SyntaxError)return problem(400,"invalid_json","Request body must be valid JSON.",correlationId);if(error instanceof AuthenticationError)return problem(401,"unauthorized",error.message,correlationId);if(error instanceof MembershipError||error instanceof AuthorizationError)return problem(403,"forbidden","You do not have permission to update inventory.",correlationId);if(error instanceof InventoryLifecycleValidationError)return NextResponse.json({error:"invalid_request",message:error.message,issues:error.issues,correlationId},{status:400,headers:{"cache-control":"no-store","x-correlation-id":correlationId}});if(error instanceof InventoryLifecycleConflictError)return problem(409,"data_conflict",error.message,correlationId);return problem(500,"internal_error","Inventory could not be updated.",correlationId);}}function object(value:unknown):Record<string,unknown>{if(!value||typeof value!=="object"||Array.isArray(value))throw new InventoryLifecycleValidationError(["Request data must be an object."]);return value as Record<string,unknown>;}function string(value:unknown){return typeof value==="string"?value:"";}function number(value:unknown){return typeof value==="number"?value:Number.NaN;}function optional(value:unknown){return typeof value==="string"&&value.trim()?value.trim():undefined;}function optionalStatus(value:unknown){return value==="available"||value==="unavailable"?value:undefined;}function problem(status:number,error:string,message:string,correlationId:string){return NextResponse.json({error,message,correlationId},{status,headers:{"cache-control":"no-store","x-correlation-id":correlationId}});}
