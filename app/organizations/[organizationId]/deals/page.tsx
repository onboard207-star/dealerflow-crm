import Link from "next/link";
import { AppShell } from "@/components/app-shell";
import { DealDirectoryReader } from "@/lib/server/deals";
import { loadDirectoryContext } from "../_lib/load-directory-context";

export const dynamic = "force-dynamic"; interface PageProps { params: Promise<{ organizationId: string }>; searchParams: Promise<{ q?: string; status?: string }> }
export default async function DealsPage({ params, searchParams }: PageProps) { const { organizationId } = await params; const filters = await searchParams; const context = await loadDirectoryContext(organizationId, "deal.read"); const page = await new DealDirectoryReader(context.pool).list({ userId: context.session.user.id, organizationId, locationIds: context.membership.locationIds }, { search: filters.q, status: filters.status, limit: 50 }); const base = `/organizations/${organizationId}/deals`; return <AppShell organizationId={organizationId} navigationCapabilities={context.membership.capabilities} activeHref={base} breadcrumbs={[{ label: context.organization.name }, { label: "Deals" }]} user={{ name: context.session.user.name, email: context.session.user.email, ...(context.session.user.image ? { image: context.session.user.image } : {}) }}><section className="mx-auto max-w-7xl" aria-labelledby="deals-heading"><div><h1 id="deals-heading" className="text-2xl font-semibold tracking-tight">Deals</h1><p className="mt-1 text-sm text-muted-foreground">Current lead-to-sale workflows across your assigned locations.</p></div><form action={base} className="mt-5 grid gap-3 rounded-xl border bg-card p-4 sm:grid-cols-[minmax(0,1fr)_12rem_auto]"><label className="space-y-1 text-xs font-medium text-muted-foreground">Deal search<input name="q" defaultValue={filters.q} maxLength={100} placeholder="Customer, deal number, or VIN" className="focus-ring block h-10 w-full rounded-lg border bg-background px-3 text-sm text-foreground" /></label><label className="space-y-1 text-xs font-medium text-muted-foreground">Status<select name="status" defaultValue={filters.status ?? ""} className="focus-ring block h-10 w-full rounded-lg border bg-background px-3 text-sm text-foreground"><option value="">All statuses</option>{["draft", "working", "pending-approval", "approved", "contracted", "delivered", "cancelled"].map((status) => <option key={status} value={status}>{status.replace("-", " ")}</option>)}</select></label><button className="focus-ring h-10 self-end rounded-lg bg-primary px-4 text-sm font-medium text-primary-foreground">Apply</button></form><div className="mt-5 overflow-hidden rounded-xl border bg-card shadow-soft">{page.records.length ? <ul className="divide-y" role="list">{page.records.map((deal) => <li key={deal.id}><Link href={`/organizations/${organizationId}/deals/${deal.id}/quotes`} className="focus-ring grid gap-3 p-4 hover:bg-muted/40 sm:grid-cols-[minmax(13rem,2fr)_repeat(3,minmax(8rem,1fr))] sm:items-center sm:p-5"><span className="min-w-0"><span className="block truncate text-sm font-medium">{deal.customerName}</span><span className="block truncate text-xs text-muted-foreground">{deal.dealNumber} · {deal.vehicleLabel}</span></span><Fact label="Status" value={deal.status} /><Fact label="Purchase" value={deal.purchaseType ?? "Undecided"} /><Fact label="Agreed price" value={deal.agreedPriceCents !== undefined ? price(deal.agreedPriceCents) : "Not set"} /></Link></li>)}</ul> : <div className="p-8 text-center text-sm text-muted-foreground">No deals match these filters.</div>}</div></section></AppShell>; }
function Fact({ label, value }: { label: string; value: string }) { return <span><span className="block text-[11px] text-muted-foreground sm:sr-only">{label}</span><span className="block truncate text-sm capitalize">{value}</span></span>; } function price(cents: number) { return new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 0 }).format(cents / 100); }
